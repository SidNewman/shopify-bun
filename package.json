{
  "name": "shopify-bun",
  "author": "Sid Newman",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bunx concurrently \"bun build theme/index.ts --outdir=assets --watch\" \"shopify theme dev --password=$THEME_ACCESS_PASSWORD\"",
    "build": "bun build theme/index.ts --outdir=assets"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}